import 'package:flutter/material.dart';

class FlutterbaseSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 8.0,
      height: 8.0,
    );
  }
}

class FlutterbaseHalfSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 4.0,
      height: 4.0,
    );
  }
}

class FlutterbaseBigSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 24.0,
      height: 24.0,
    );
  }
}

class FlutterbasePageSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 12.0,
      height: 12.0,
    );
  }
}
